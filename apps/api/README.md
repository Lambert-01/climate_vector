# Climate Vector API

Python API layer for the climate-vector prototype.

Planned runtime:

```bash
uvicorn app.main:app --reload --port 8000
```

The first implementation reads prepared CSV outputs from `data/processed` and `outputs/tables`.

